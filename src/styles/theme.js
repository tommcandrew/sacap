import { createMuiTheme } from "@material-ui/core/styles"

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#587d71",
    },
    secondary: {
      main: "#ffeb3b",
    },
    action: {
      main: "#88a09e",
    },
  },
})

export default theme
